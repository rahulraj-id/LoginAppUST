<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:label="LoginSample"
        android:theme="@style/Theme.LoginSample">

        <activity android:name=".presentation.auth.RedirectUriReceiverActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="myapp" android:host="oauth2redirect" />
            </intent-filter>
        </activity>

        <activity android:name=".presentation.detail.DeviceDetailActivity" />
        <activity android:name=".presentation.home.HomeActivity" />
        <activity android:name=".presentation.auth.LoginActivity" />
        <activity android:name=".presentation.auth.SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
